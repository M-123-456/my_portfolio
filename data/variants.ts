import { INavVariants } from '../types';

export const navVariants: INavVariants = {
    hidden: {
        // after:''
        // width:0 
    },
    visible:{
        width: "100%"
    },
    transition: {
        ease: 'easeInOut'
    }
}